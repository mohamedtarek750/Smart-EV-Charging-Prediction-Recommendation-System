# The committed models in artifacts/models/ were pickled with the versions noted in
# artifacts/reports/metrics.json ("environment").  Pickled estimators are NOT portable
# across major numpy / scikit-learn versions - if yours differ, just retrain:
#     python -m src.models.train        (~40 seconds)

# Core
numpy>=2.0
pandas>=2.1
scikit-learn>=1.6
scipy>=1.11
joblib>=1.3

# Storage / plots
pyarrow>=14.0
plotly>=5.20
matplotlib>=3.8

# Apps
streamlit>=1.36
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
